# Railway Configuration for RewardHub

[build]
dockerfile = "Dockerfile"

[deploy]
# Environment variables for Spring Boot
[env]
# Railway will automatically set PORT - Spring Boot reads it as server.port
SPRING_PROFILES_ACTIVE = "production"
